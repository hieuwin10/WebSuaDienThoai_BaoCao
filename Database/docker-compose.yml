services:
  mongodb:
    image: mongo:latest
    container_name: phone_repair_mongodb
    restart: always
    ports:
      - "27017:27017"
    command: ["mongod", "--replSet", "rs0", "--bind_ip_all"]
    volumes:
      - mongodb_data:/data/db
    networks:
      - phone_repair_net

volumes:
  mongodb_data:
    driver: local

networks:
  phone_repair_net:
    driver: bridge
