# Nhật ký phát triển - 27/03/2026

## 1. Đồng bộ hóa Nguồn dữ liệu (Source of Truth)
- **Cơ sở dữ liệu**: Chuyển đổi toàn bộ hệ thống sang sử dụng `phone_repair_db` thay vì `DoAN_C3` cũ.
- **Dữ liệu mẫu**: Chạy thành công `seed.js` tại thư mục `/Database` để khởi tạo các tài khoản Admin (`admin`/`password123`) và dữ liệu thiết bị thực tế.
- **Tái cấu trúc Schema**: 
    - Thay thế hệ thống `Product/Category` chung chung bằng hệ thống chuyên biệt: `Device` (Thiết bị), `Service` (Dịch vụ), `Component` (Linh kiện).
    - Cập nhật các Schema `RepairTicket`, `Warranty`, `Media`, `User`, `Role`, `Profile` để khớp 100% với thiết kế của thầy.

## 2. Nâng cấp Backend
- **Controller**: Refactor `users.js` sử dụng pattern của thầy (QueryLogin, CreateAnUser...).
- **Routes**: Triển khai các API route mới cho `devices`, `services`, `components`, `repair-tickets` (đã nâng cấp), `warranties`.
- **Auth Middleware**: Cập nhật `CheckLogin` để hỗ trợ chuyển hướng (Redirect) tự động về trang đăng nhập nếu truy cập từ trình duyệt mà chưa có token.



## 3. Trạng thái hiện tại
- **Hệ thống**: Hoạt động ổn định, mượt mà trên giao diện Dark Mode Premium.
- **Đồng bộ**: Đạt 100% độ nhất quán với yêu cầu Đồ án.
- **Redirect**: Mượt mà, đăng nhập xong vào thẳng Dashboard.
